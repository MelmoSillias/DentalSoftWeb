doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '16'

        profiling_collect_backtrace: false
        use_savepoints: true
    orm:
        auto_generate_proxy_classes: true
        enable_lazy_ghost_objects: true
        report_fields_where_declared: true
        validate_xml_mapping: true
        naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
        identity_generation_preferences:
            Doctrine\DBAL\Platforms\PostgreSQLPlatform: identity
        auto_mapping: true
        mappings:
            Billing:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Billing/Entity'
                prefix: 'App\Billing\Entity'
                alias: Billing
            CareDelivery:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/CareDelivery/Entity'
                prefix: 'App\CareDelivery\Entity'
                alias: CareDelivery
            ClinicalRecord:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/ClinicalRecord/Entity'
                prefix: 'App\ClinicalRecord\Entity'
                alias: ClinicalRecord
            Communication:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Communication/Entity'
                prefix: 'App\Communication\Entity'
                alias: Communication
            IdentityAccess:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/IdentityAccess/Entity'
                prefix: 'App\IdentityAccess\Entity'
                alias: IdentityAccess
            Inventory:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Inventory/Entity'
                prefix: 'App\Inventory\Entity'
                alias: Inventory
            Patient:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Patient/Entity'
                prefix: 'App\Patient\Entity'
                alias: Patient
            Scheduling:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Scheduling/Entity'
                prefix: 'App\Scheduling\Entity'
                alias: Scheduling
            Settings:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Settings/Entity'
                prefix: 'App\Settings\Entity'
                alias: Settings
        controller_resolver:
            auto_mapping: false

when@test:
    doctrine:
        dbal:
            # "TEST_TOKEN" is typically set by ParaTest
            dbname_suffix: '_test%env(default::TEST_TOKEN)%'

when@prod:
    doctrine:
        orm:
            auto_generate_proxy_classes: false
            proxy_dir: '%kernel.build_dir%/doctrine/orm/Proxies'
            query_cache_driver:
                type: pool
                pool: doctrine.system_cache_pool
            result_cache_driver:
                type: pool
                pool: doctrine.result_cache_pool

    framework:
        cache:
            pools:
                doctrine.result_cache_pool:
                    adapter: cache.app
                doctrine.system_cache_pool:
                    adapter: cache.system
