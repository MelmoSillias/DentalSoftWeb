; Allow larger medical imaging uploads (cephalometry, panoramics, etc.)
; upload_max_filesize = per file, post_max_size = whole request (multiple files)
upload_max_filesize = 50M
post_max_size = 256M
max_file_uploads = 30
